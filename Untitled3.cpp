#include <stdio.h>
int main()
{
	int a;
	scanf("%d", &a);
	if ((a>0) && (a!=0))  
		{
			printf("a la so duong");
		}
	else
		if	((a>0) && (a!=0))		
			{
				printf("a la so am");
			}
		
	return 0;
}
